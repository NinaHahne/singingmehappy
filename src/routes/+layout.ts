// This can be false if you're using a fallback (i.e. SPA mode)
export const prerender = true;

// Wichtig für Netcup/Apache: erzeugt /foo/index.html statt foo.html
export const trailingSlash = 'always';
